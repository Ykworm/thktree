---
name: freemode
description: 切换到 freemode 档位（微改/试验模式）。跳过 brainstorm→草稿→writing-plans 规划流程，直接实现并自测。触发词：freemode。委托给 AGENTS.md 档位选择器，不复制流程。
---
切换到 **freemode** 档位（定义见 `AGENTS.md` 档位选择器）：这是微改 / 试验模式，跳过 `brainstorm→草稿→writing-plans` 的规划流程，直接实现并自测即可。适合小修小补、本地试验、一次性脚本。完成后如需回填文档，用 `ctsync`。注意 freemode 不写架构决策、不做 worktree 隔离——高风险或架构级改动请改用 standard / rigorous。
