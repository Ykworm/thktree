# 树形 LLM 对话 App — 产品需求说明（MVP）

| 属性 | 说明 |
|------|------|
| 文档版本 | 1.0 |
| 更新日期 | 2026-05-25 |
| 平台 | iOS 为主（Flutter 工程可保留 Android 目录，MVP 仅验证 iOS） |
| LLM | DeepSeek V4（BYOK，用户自备 API Key） |

---

## 1. 产品概述

### 1.1 目标

构建一款 **本地优先** 的 iOS App：用户围绕多个 **主题（Theme）** 组织工作；每个主题下是一棵 **树形 Session**；每个 Session 节点代表一段用户与 LLM 的多轮对话。子节点可继承祖先上下文（通过用户确认过的 **总结**），并支持从对话或 **笔记** 派生新分支。

### 1.2 核心价值

- **树形对话**：同一主题下按分支探索不同思路，而非单一线性聊天记录。
- **可控上下文**：祖先历史不直接全量灌入模型，而是通过可多轮打磨的 **总结** 注入，兼顾 token 限制与语义完整。
- **笔记与对话联动**：摘录对话片段为笔记；笔记既可发起新 Session，也可插入当前对话。
- **可读可接力**：正文以 Markdown 文件存储，关系用 SQLite 索引，便于人工查看与其他 IDE/Agent 继续开发。

### 1.3 用户假设

- 个人使用为主；API Key 由用户在 App 内配置。
- 不需要账号体系、云同步、导出备份（MVP 明确不做）。

---

## 2. MVP 范围

### 2.1 必须包含

| 能力 | 说明 |
|------|------|
| 多主题 | 首页列出多个 Theme；进入某 Theme 后见其 Session 树 |
| 树形 Session | 节点可嵌套；`parentId` 为空表示主题下 **根节点** |
| 对话 | 每节点一个 chat thread；DeepSeek V4 流式回复 |
| 本地持久化 | 文本（Markdown）为正文真相源；SQLite 存关系与元数据 |
| 子孙视图 | 选中节点后可 **过滤只看子孙** |
| 汇总预览 | 对子孙节点做简单汇总预览（如最后一条消息截断） |
| 笔记（双场景） | 见 [§4 笔记](#4-笔记) |
| 祖先上下文总结 | 见 [§5 祖先上下文总结](#5-祖先上下文总结) |
| 设置 | DeepSeek API Key、模型 ID（可编辑，适配 V4 官方 model 名） |

### 2.2 明确不做（v1 / MVP）

- 删除、归档
- 导出 / 备份
- 多厂商 LLM（仅 DeepSeek V4）
- iCloud / 多端同步
- Android 发布级适配（可不测）

---

## 3. 核心概念与数据

### 3.1 术语

| 术语 | 定义 |
|------|------|
| **Theme（主题）** | 顶层容器，内含一棵 Session 树 |
| **SessionNode（节点）** | 树中一个节点；`kind` 为 `chat`（普通对话）或 `summary`（打磨祖先总结的辅助对话） |
| **Session** | 某节点上的多轮 User/Assistant 消息，持久化于该节点的 `session.md` |
| **Note（笔记）** | 用户创建的摘录/备忘，持久化于 `notes/{id}.md` |
| **ContextSummary（祖先总结）** | 用户确认后写入的 `context-summary.md`，供子节点对话注入 |

### 3.2 树语义

- 每个 **chat** 节点 = 独立对话线程（多轮消息）。
- 子节点的 LLM 请求应携带：已 approve 的 **祖先总结** + 本节点 `session.md` 历史（见 §5）。
- **无删除**：仅追加写入；未来若需要删除再单独立项。
- 从 UI 上，用户可在某节点视角 **查看其所有子孙** 并 **汇总预览**。

### 3.3 存储原则

- **正文**：Markdown 文件（人类可读、Agent 可 rg）。
- **关系与检索**：SQLite（主题、节点父子、消息元数据、笔记索引等）。
- **并发写**：单写者；流式输出使用临时文件或 `<!-- streaming -->` 标记，避免写坏文件。

详细文件格式见 [docs/storage-format.md](./storage-format.md)（实现时维护；v0.1 定稿）。

### 3.4 目录布局（逻辑）

```text
{appDocuments}/
  themes/{themeId}/
    theme.meta.json
    nodes/{nodeId}/
      node.meta.json
      session.md
      context-summary.md    # 可选
    notes/{noteId}.md
```

---

## 4. 笔记

### 4.1 创建与累积选段

1. 用户在 **聊天气泡** 内选择一段文字。
2. 可先 **创建新笔记**，再将选段 **添加到笔记**（笔记 `body` 可多次累积）。
3. 笔记记录 `sourceNodeId`（创建时所在节点，可选）。

### 4.2 场景 A：笔记作为新对话的源头

**入口**：笔记详情 →「以此发起对话」

**流程**：

1. 进入 **撰写首条消息** 界面（`ComposeFirstMessageScreen`）。
2. 输入框 **预填笔记正文**；用户可：
   - 全部删除后重写；
   - 在笔记前后追加自己的问题或说明；
   - 完全不使用笔记内容。
3. **可选** 选择挂载的 `parentNode`：
   - 选择某节点 → 新节点 `parentId` 指向该节点；
   - **不选择** → 新节点为 Theme 下 **根节点**（`parentId = null`）。
4. 用户点击 **发送** 后系统才：
   - 创建 `SessionNode`；
   - 将 **用户编辑后的全文** 写入 `session.md` 第一条 `user` 消息；
   - 调用 DeepSeek 获取回复。
5. 发送成功后，可记录 `Note.spawnedNodeId`。

**原则**：笔记内容仅为 **草稿预填**，**不得**自动发送。

### 4.3 场景 B：笔记插入当前对话

**入口**：当前打开的 **chat 节点** 对话页 →「插入笔记」

**流程**：

1. 将笔记正文 **预填到当前输入框**（可编辑）。
2. 用户点击 **发送** 后，内容作为一条 `user` message 写入当前节点的 `session.md`，再请求 LLM。

**原则**：不创建新节点；同样 **不得**自动发送。

### 4.4 场景 A 与 B 对比

| | 场景 A | 场景 B |
|---|--------|--------|
| 是否新建节点 | 是（发送后） | 否 |
| parent | 可选 | 不适用 |
| 预填来源 | 笔记正文 | 笔记正文 |
| 发送前可编辑 | 是 | 是 |

---

## 5. 祖先上下文总结

### 5.1 问题

子节点若直接携带全部祖先对话，易超 token；若摘要过短，又可能丢失含义。

### 5.2 方案

为需要祖先上下文的子节点提供 **总结打磨流程**：

1. 系统读取祖先链上各节点的 `session.md`（总结用 Session 可读 **全量** 祖先正文）。
2. DeepSeek 生成 **初稿总结**。
3. 用户在 **`kind: summary`** 的辅助节点中进行 **多轮对话**，修改直到满意。
4. 用户 **确认采用** → 写入该子节点（或目标节点）的 `context-summary.md`（`approvedText`、`stale=false` 等）。
5. 子节点 **正常 chat** 时，prompt 仅注入 **已确认的总结** + 本节点历史。

### 5.3 过期（stale）

- 祖先链上任意节点新增消息后，相关总结标记 `stale=true`。
- UI 提示用户是否 **重新总结**；不强制阻断发消息，但应明显提示上下文可能过时。

### 5.4 兜底

若总结不足，用户可：

- 从 **笔记** 插入补充内容到当前对话（场景 B）；
- 或从历史消息中复制选段（UI 层支持选段即可，MVP 可不单独做「引用消息」实体）。

---

## 6. 界面与导航（MVP）

无外部设计稿；实现阶段用 **Material 3** 快速搭建，`v0.6` 可选 Cupertino 外壳优化 iOS 观感。

| # | 界面 | 主要能力 |
|---|------|----------|
| 1 | 主题列表 | 新建 Theme、进入 Theme |
| 2 | Theme 内 Session 树 | 展开/选中节点；进入节点详情 |
| 3 | 节点详情 | Tab：**对话** / **子孙** / **汇总** |
| 4 | 对话页 | 流式消息；选段→笔记；插入笔记（场景 B） |
| 5 | 笔记列表/详情 | 管理笔记；以此发起对话（场景 A） |
| 6 | 撰写首条消息 | 场景 A 专用；预填 + 可选 parent |
| 7 | 设置 | API Key（安全存储）、模型 ID |
| 8 | 总结向导 | `stale` 或首次需要时进入 summary 流 |

---

## 7. LLM 集成

| 项 | 要求 |
|----|------|
| 提供商 | DeepSeek |
| 模型 | V4（具体 `model` 字符串以官方文档为准，设置页可配置） |
| 密钥 | BYOK，`flutter_secure_storage`，不得写入仓库 |
| 传输 | HTTP + SSE 流式 |
| 子节点 Prompt 结构 | system 说明 + `context-summary.md` 已确认正文 + 本节点 `session.md` 历史 |

---

## 8. 技术约束（实现参考）

| 层 | 选型 |
|----|------|
| 框架 | Flutter 3.x |
| 状态 | Riverpod |
| 路由 | go_router |
| 正文 | Markdown + path_provider |
| 索引 | sqflite |
| 安全存储 | flutter_secure_storage |
| HTTP | dio |

---

## 9. 版本计划（按日交付）

目标：**一天一个或多个可运行版本**；每版结束更新 `PROGRESS.md` / `CHANGELOG.md` 并 git 提交，便于其他 Agent 接力。

| 版本 | 交付物 |
|------|--------|
| **v0.1** | Flutter 工程骨架；`AGENTS.md`、`PROGRESS.md`、`CHANGELOG.md`；`docs/storage-format.md` 定稿；空路由；本地目录初始化 |
| **v0.2** | SQLite 索引；Theme/Node CRUD；树 UI；子孙过滤；汇总预览 |
| **v0.3** | `session.md` 读写与流式 append；设置页；DeepSeek 对话 |
| **v0.4** | 笔记选段；场景 A（撰写首条 + 可选 parent）；场景 B（插入当前对话） |
| **v0.5** | 祖先总结流程；`context-summary.md`；stale；子节点注入 |
| **v0.6** | iOS 真机；键盘与安全区；API/网络错误态 |

---

## 10. 仓库交接文件（实现后维护）

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | Agent 须知：如何构建、存储约定、禁止事项 |
| `PROGRESS.md` | 当前版本、Done、**Next（接力入口）**、已知问题 |
| `CHANGELOG.md` | 每版本变更一行 |
| `docs/storage-format.md` | Markdown / SQLite schema 权威说明 |
| `docs/REQUIREMENTS.md` | 本文档 |

---

## 11. 风险

| 风险 | 缓解 |
|------|------|
| Markdown 并发写损坏 | 单 isolate 写盘；流式 tmp + rename |
| 列表性能 | 预览用 SQLite `messages_meta`，避免每次全文 parse |
| 总结超 token | MVP 截断 + 警告；v0.7+ 可考虑分段 rollup |
| Agent 接力断层 | 强制每版更新 `PROGRESS.md` |

---

## 12. 待产品确认（可选，不阻塞 v0.1）

- 工程 bundle id / 显示名称
- DeepSeek V4 正式 `model` 字段字符串
- Theme / Node 默认标题命名规则

---

## 13. 功能路线图

### 13.1 已完成

| 功能 | 说明 |
|------|------|
| 多主题管理 | 创建、进入主题 |
| 树形对话 | 节点嵌套，分支探索不同思路 |
| 修改对话 title | 节点重命名 dialog |
| 删除对话/节点 | 长按菜单 + swipe 删除，`deleteNodeSubtree` 递归删除 |
| 重新生成 | 最后一条 LLM 回复重试（`retryLastMessage`） |
| Face ID 锁 | 全 App 生物认证，`AuthGate` + `WidgetsBindingObserver`，进前台弹验证 |
| 流式对话 | LLM 流式回复 + Stop 控制 |
| 笔记系统 | 创建、编辑、累积选段、重命名 |
| 笔记从零新建 | 笔记总览页 ➜ 新建笔记（进入"未分类"主题） |
| 从笔记创建分支 | 笔记内容 → 新对话首条消息 |
| 模型选择 | 对话级覆盖 + 全局设置 |
| 自定义 system prompt | 对话级 system prompt |
| 标题建议 | LLM 生成对话标题（手动触发 + 独立模型配置） |
| 多 LLM 提供商 | 多 provider / API Key 管理 |
| Stop/Send 状态管理 | 乐观更新 + 磁盘自愈 |
| 分支模式选择 | 摘要模式 / 原始模式 |
| 笔记删除 | `NoteStore.deleteNote()` + 详情页 AppBar 垃圾桶 + 列表 swipe-to-delete |
| 笔记全选复制 | 笔记内容支持全选 + 复制 |
| 对话排序 | 拖拽排序 / 按最近活跃排序 |
| 消息复制 | 单条消息复制 / 多条消息批量复制 |
| Vision + AI 绘图 | assistant 消息分享图标 → 问答对卡片截图 → 系统分享面板 |

### 13.2 待实现

#### 高优先级

| 功能 | 说明 |
|------|------|
| 笔记搜索/过滤 | 笔记列表页无搜索入口 |

#### 中优先级

| 功能 | 说明 |
|------|------|
| 导出/备份 | zip + manifest → 分享面板，目录树结构人类可读 |
| 导入恢复 | zip → manifest → 恢复到 app（round-trip） |
| 剪贴板快速发送 | 复制文字后进 app 浮现"发送给 LLM？"提示 |

#### 低优先级

| 功能 | 说明 |
|------|------|
| 网络错误重试 | API 失败时的用户提示 |
| 祖先上下文摘要 | 子节点继承祖先摘要，节省 token |
| 思维碰撞 | 跨主题关键词随机碰撞，本地零 token，感兴趣才调 LLM |

---

## 附录 A：消息块格式（摘要）

完整定义见 `storage-format.md`。

```markdown
---
schema: session/v1
nodeId: "..."
themeId: "..."
kind: chat
parentId: null
title: "..."
---

## user · 2026-05-25T12:01:00Z · msg_a1b2
消息正文（可多行）

## assistant · 2026-05-25T12:01:03Z · msg_c3d4
回复正文
```

---

## 附录 B：修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-05-25 | 初稿：合并对话结论；笔记场景 A parent 可选、首条可编辑；文本优先存储 |
| 1.1 | 2026-06-05 | 路线图更新：完成笔记全选复制、对话排序、消息复制、Vision + AI 绘图（分享为图片）；移除不做的功能 |
