# API Key / 敏感信息泄露检查报告

## 检查时间
2026-07-27

## 检查范围
全项目文件扫描（排除 node_modules、.git、build、.dart_tool、Pods、字体/二进制文件）

## 检查项目与结果

| 检查项 | 结果 |
|--------|------|
| 高置信度密钥格式（sk-xxx、ghp_xxx、AKIAxxx、AIzaxxx 等） | 未发现 |
| .env 文件（含 .env.local / .env.production 等） | 不存在 |
| 凭据文件（credentials.json、service-account、.pem、.key、.p12） | 不存在 |
| Firebase/Google 配置文件（google-services.json、GoogleService-Info.plist） | 不存在 |
| 硬编码 Auth Header（Bearer/Basic/Token） | 未发现 |
| Dart 文件中的硬编码密钥/长随机字符串 | 未发现 |
| AI/云厂商 Provider Key（OpenAI/Anthropic/阿里/百度/腾讯等） | 未发现 |
| 配置文件中的敏感字段（json/yaml/toml/cfg） | 未发现 |
| Git 历史中曾经提交过的密钥文件 | 未发现 |
| .gitignore 是否正确排除 .env | 已正确配置 |

## .gitignore 状态
```
.env
.env.*
!.env.example
```
配置正确，`.env` 文件已被排除。

## 结论
**项目当前没有发现任何 API Key 或敏感信息泄露风险。**

## 建议（预防性）
1. 项目的 `.gitignore` 已正确配置，继续维护即可
2. 如未来需要使用 LLM API Key，建议通过 `--dart-define-from-file` 或本地 `.env` 文件注入，不要硬编码
3. 定期检查 `git log -p -S 'sk-'` 等命令确认历史提交中没有遗留
